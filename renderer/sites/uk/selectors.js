'use strict';

const UK_SELECTORS = {
  coupon: [
    'P#pqv-price-coupon-message',
    '[id^="greenBadgepct"]',
  ]
};

if (typeof module !== 'undefined' && module.exports) module.exports = UK_SELECTORS;
