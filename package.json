{
  "name": "amazon-patrol",
  "version": "1.1.7",
  "description": "亚马逊监控助手桌面版",
  "main": "electron/main.js",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10"
  },
  "scripts": {
    "start": "cross-env NODE_OPTIONS= electron . --dev",
    "build:win": "electron-builder --win",
    "build:mac": "electron-builder --mac",
    "build:all": "electron-builder --win --mac"
  },
  "dependencies": {
    "electron-updater": "^6.3.9",
    "node-schedule": "^2.1.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^43.2.0",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.amazonpatrol.desktop",
    "productName": "亚马逊监控助手",
    "artifactName": "amazon-patrol-${version}.${ext}",
    "directories": {
      "output": "dist"
    },
    "publish": {
      "provider": "github",
      "owner": "hzxuncle",
      "repo": "amazon-patrol-electron"
    },
    "files": [
      "electron/**/*",
      "renderer/**/*",
      "assets/**/*"
    ],
    "asarUnpack": [
      "renderer/content.js",
      "renderer/selectors.js",
      "renderer/selectors/**/*",
      "renderer/sites/**/*"
    ],
    "win": {
      "target": [
        { "target": "nsis", "arch": ["x64"] },
        { "target": "zip",  "arch": ["x64"] }
      ],
      "icon": "assets/icons/icon.ico",
      "sign": null
    },
    "mac": {
      "artifactName": "amazon-patrol-${version}-${arch}-mac.${ext}",
      "target": [
        { "target": "zip", "arch": ["x64", "arm64"] }
      ],
      "icon": "assets/icons/icon.icns"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "allowElevation": true,
      "installerLanguages": ["zh_CN"],
      "language": "2052",
      "include": "build/installer.nsh",
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "亚马逊监控助手"
    }
  }
}
